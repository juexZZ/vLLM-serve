[project]
name = "vllm-env"
version = "0.1.0"
description = "Deploy LLMs with vLLM."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.8.0",
    "torchvision>=0.23.0",
    "tqdm>=4.67.1",
    "vllm>=0.11.0",
    "qwen-vl-utils==0.0.14"
]
